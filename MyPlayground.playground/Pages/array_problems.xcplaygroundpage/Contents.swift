import Foundation
/*: 
 [Previous](@previous)
 ## Practice 1
 Fill out the following `array` with a few `elements`.
 */
var colors = <#list some colors here#>
//:here is how many colors you listed.
colors.count
//:Write code that checks if `color` is in your `array`. hint: use a for-loop.
var color = "blue"

//: [Next - More Array Practice](@next)
