import Foundation
/*: 
 [Previous](@previous)
 ## Practice 1
 Edit the following loop so it adds up the numbers from `1 to 15`.
 */
var sum = 0
for i in <#range#> {
    sum += i
}
sum
//:Have this loop add up the numbers from `25 to 50`.
sum = 0
for i in <#range#> {
    sum += i
}
sum
/*:
 ## Practice 2
 Write a loop that produces a string of the numbers from `n to 0`.
 \
 `(n = 5)` results in  `"5, 4, 3, 2, 1"`
 */
var n = 5
var countdown = ""

//: [Next](@next)
